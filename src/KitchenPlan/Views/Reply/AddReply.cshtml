@model KitchenPlan.BlogReply

<h3>You're replying to:</h3>

<br />
<h3>Type your reply</h3>
@using (Html.BeginForm("AddReply", "Reply"))
{ 
    @Html.EditorForModel()
    <input name="Add" type="submit" />
}